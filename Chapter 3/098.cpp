#include <iostream>
using namespace std;

int main ()
{
    cout << "a, b, c: ";
    double a, b, c;
    cin >> a >> b >> c;

    cout << "d, e, f: ";
    double d, e, f;
    cin >> d >> e >> f;

    return 0;
}